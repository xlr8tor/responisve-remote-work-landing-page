import styled from "styled-components";

export const Wrapper = styled.main`
  .main__image {
    display: block;
    width: fit-content;
  }

  .main__content {
    text-align: center;
  }

  .title {
    font-size: var(--big-font-size);
  }

  .description {
    color: var(--text-color);
    font-weigth: var(--font-size-medium);
  }
`;
